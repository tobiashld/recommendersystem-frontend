import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders landingpage', () => {
  // render(<App />);
  // const linkElement = screen.getByTitle('Recommendersystem')
  // expect(linkElement).toBeInTheDocument()
});
